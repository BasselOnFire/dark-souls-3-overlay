const KEY="ds3-overlay-state-v2",OLD="ds3-overlay-state-v1",DEFAULT={players:[{id:1,name:"Bassel",deaths:0},{id:2,name:"Kevin",deaths:0}],elapsed:0,running:false,startedAt:null};let state=load();function load(){try{let raw=localStorage.getItem(KEY)||localStorage.getItem(OLD);return raw?{...DEFAULT,...JSON.parse(raw)}:JSON.parse(JSON.stringify(DEFAULT))}catch{return JSON.parse(JSON.stringify(DEFAULT))}}function save(){localStorage.setItem(KEY,JSON.stringify(state));renderOverlay();try{new BroadcastChannel("ds3-overlay").postMessage("update")}catch{}}function total(){return state.elapsed+(state.running&&state.startedAt?Math.floor((Date.now()-state.startedAt)/1000):0)}function fmt(s){s=Math.max(0,Math.floor(s));return String(Math.floor(s/3600)).padStart(3,"0")+":"+String(Math.floor(s%3600/60)).padStart(2,"0")+":"+String(s%60).padStart(2,"0")}function esc(s){return String(s).replace(/[&<>"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]))}function renderOverlay(){let t=document.getElementById("timer");if(t)t.textContent=fmt(total());let p=document.getElementById("players");if(p)p.innerHTML=state.players.map(x=>'<div class="player-card"><span class="player-name">'+esc(x.name)+'</span><span class="death"><span class="skull">☠</span>'+x.deaths+'</span></div>').join("");let ct=document.getElementById("controlTimer");if(ct)ct.textContent=fmt(total())}function renderControls(){let pc=document.getElementById("playerControls");if(!pc)return;pc.innerHTML=state.players.map(x=>'<div class="player-control" data-row="'+x.id+'"><label class="name-label">Name<input data-name="'+x.id+'" value="'+esc(x.name)+'"></label><label class="deaths-label">Tode<input data-deaths="'+x.id+'" type="number" min="0" value="'+x.deaths+'"></label><button data-minus="'+x.id+'">−1</button><button data-plus="'+x.id+'">+1</button><button class="remove" data-remove="'+x.id+'">Löschen</button></div>').join("")}function syncTimeInputs(){let s=total(),h=document.getElementById("hours"),m=document.getElementById("minutes"),se=document.getElementById("seconds");if(h)h.value=Math.floor(s/3600);if(m)m.value=Math.floor(s%3600/60);if(se)se.value=s%60}function flash(){let x=document.getElementById("savedMsg");if(x){x.textContent="Gespeichert ✓";setTimeout(()=>x.textContent="",1600)}}document.addEventListener("click",e=>{let b=e.target.closest("button");if(!b)return;if(b.id==="startTimer"&&!state.running){state.running=true;state.startedAt=Date.now();save()}else if(b.id==="pauseTimer"&&state.running){state.elapsed=total();state.running=false;state.startedAt=null;save();syncTimeInputs()}else if(b.id==="resetTimer"&&confirm("Spielzeit wirklich auf 0 setzen?")){state.elapsed=0;state.running=false;state.startedAt=null;save();syncTimeInputs()}else if(b.id==="setTime"){let h=+document.getElementById("hours").value||0,m=Math.min(59,+document.getElementById("minutes").value||0),s=Math.min(59,+document.getElementById("seconds").value||0);state.elapsed=Math.max(0,h*3600+m*60+s);state.startedAt=state.running?Date.now():null;save();flash()}else if(b.id==="addPlayer"){state.players.push({id:Date.now(),name:"Spieler "+(state.players.length+1),deaths:0});save();renderControls()}else if(b.id==="saveAll"){document.querySelectorAll("[data-name]").forEach(i=>{let x=state.players.find(p=>p.id==i.dataset.name);if(x)x.name=i.value.trim()||"Spieler"});document.querySelectorAll("[data-deaths]").forEach(i=>{let x=state.players.find(p=>p.id==i.dataset.deaths);if(x)x.deaths=Math.max(0,parseInt(i.value)||0)});save();renderControls();flash()}else if(b.dataset.plus||b.dataset.minus){let id=b.dataset.plus||b.dataset.minus,x=state.players.find(p=>p.id==id);if(x)x.deaths=Math.max(0,x.deaths+(b.dataset.plus?1:-1));save();renderControls()}else if(b.dataset.remove&&state.players.length>1){state.players=state.players.filter(p=>p.id!=b.dataset.remove);save();renderControls()}});window.addEventListener("storage",e=>{if(e.key===KEY){state=load();renderOverlay();renderControls()}});try{let bc=new BroadcastChannel("ds3-overlay");bc.onmessage=()=>{state=load();renderOverlay();renderControls()}}catch{}setInterval(renderOverlay,250);renderOverlay();renderControls();syncTimeInputs();